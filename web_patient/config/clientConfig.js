/**
 * A minimal configuration needed to configure a client.
 *
 * Designed for script to be replaced in deployed containers.
 */
const CLIENT_CONFIG = {
    flaskBaseUrl: 'http://127.0.0.1:4000/',
};
